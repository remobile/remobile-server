import session from './session';
import administrator from './administrator';
import administrators from './administrators';
import administratorsCount from './administrators-count';

export default {
    administrator,
    administrators,
    administratorsCount,
    session
};
