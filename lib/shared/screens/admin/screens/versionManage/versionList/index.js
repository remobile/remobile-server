import React, {PropTypes} from 'react';
import VersionManage from './components';

export default class VersionManageContainer extends React.Component {
    render () {
        return (
            <VersionManage />
        );
    }
}
