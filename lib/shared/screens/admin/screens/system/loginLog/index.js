import React, {PropTypes} from 'react';
import LoginLog from './components';

export default class LoginLogContainer extends React.Component {
    render () {
        return (
            <LoginLog />
        );
    }
}
