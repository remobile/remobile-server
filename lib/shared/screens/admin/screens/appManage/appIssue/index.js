import React, {PropTypes} from 'react';
import AppManage from './components';

export default class AppManageContainer extends React.Component {
    render () {
        return (
            <AppManage />
        );
    }
}
